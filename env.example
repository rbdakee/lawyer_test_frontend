# Backend API URL (будет установлен на Vercel после деплоя бэкенда)
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com

# SECRET_LOCAL_TOKEN для защиты API (должен совпадать с backend)
# ВАЖНО: Этот токен не должен быть виден в браузере!
# Используется только в server-side API routes
SECRET_LOCAL_TOKEN=your-secret-local-token-change-in-production

